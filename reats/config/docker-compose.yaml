version: "3.7"

services:
  web:
    build:
      context: ..
      dockerfile: config/Dockerfile
    ports:
      - "8000:8000"
    depends_on:
      - db
    env_file:
      - .env
    volumes:
      - ../customer_app:/usr/src/app/customer_app/
      - ../cooker_app:/usr/src/app/cooker_app/
      - ../custom_renderers:/usr/src/app/custom_renderers/
      - ../delivery_app:/usr/src/app/delivery_app/
      - ../source:/usr/src/app/source/
      - ../tests:/usr/src/app/tests/
      - ../utils:/usr/src/app/utils/
    container_name: django_app

  db:
    image: postgres:15
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "8001:5432"
    env_file:
      - .env
    container_name: postgres_db

volumes:
  postgres_data:
